## REQUIREMENTS (carried forward from spec archive)

### Requirement: Daily Goals Display

`<DailyGoals>` 组件 SHALL 展示用户可设定的学习目标列表。每个目标 SHALL 包含复选框、目标描述文字、类别标签。用户勾选复选框后目标 SHALL 标记为已完成（带删除线样式）。目标数据在本次迭代中 SHALL 继续使用 mock 数据。
